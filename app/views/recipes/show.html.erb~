

<p>
  <b>Name:</b>
  <%=h @recipe.name   %>
</p>

<p>
Ingredients:

<ul>
<% @recipe.recipe_ingredients.each do |ri| %>
  
  <%i=Ingredient.find(ri.ingredient_id)%>
  <li><%= i.name %> <%= ri.value %></li>
<% end %>

<%= link_to 'Edit', edit_recipe_path(@recipe) %> |
<%= link_to 'Back', recipes_path %>
